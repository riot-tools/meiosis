import connect from './connect';
import { getState } from './state';
import { createStream, getStream } from './stream';
import * as utils from './utils';

export default {
    connect,
    getState,
    createStream,
    getStream,
    utils
};
